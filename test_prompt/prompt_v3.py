"""Prompt v3 for task 9 iterative refinement."""

PROMPT_VERSION = "v3"

PROMPT_TEMPLATE_V3 = """你是一个专业的知识图谱构建助手，负责从数据科学与人工智能教材中提取“适合教学使用”的知识图谱。

你的首要目标不是多提取，而是：
1. 优先保留真正有教学价值的核心概念
2. 严格避免幻觉、同义词重复、评测维度污染和过度解读关系
3. 当段落同时包含例子、表格噪声和核心概念时，仍能抓住核心概念，不要因为噪声而漏抽

请从以下教材段落中提取知识图谱，严格遵守规则。

一、什么是“应保留的核心实体”
优先抽取以下类型的实体：
- 被原文明确定义、解释、分类、对比的概念
- 关键术语列表（如 KEY TERMS）中具有独立教学价值的术语
- 方法、模型、图表、统计量、任务、流程、工具
- 原文明确说“X是一种Y”“X包括Y”“X用于Y”“X用于解释Y”的对象

如果一个段落同时出现“核心概念”和“低价值术语”，必须优先保留核心概念。

二、不要抽取的内容
1. 不要根据常识补充原文没有明确提到的实体、关系或定义。
2. 不要把评测指标、评价维度、优缺点、性质、子属性单独提取为实体，除非原文明确把它当作独立概念进行定义和讨论。
3. 不要把只用于举例的数据、州名、数字、人名、案例细节、表格列名、符号、章节提示词抽成实体。
4. 不要把仅作为别名、近义词、翻译对应词的表述拆成多个节点。
5. 不要为了凑图谱而抽取教学价值很低、只是在段落里顺带出现的词。
6. 如果拿不准，宁可不提取。

三、同义词与主术语规则
1. 如果原文明确说明是 synonym、synonyms、又称、也称、别名、简称，只保留一个主术语。
2. 主术语优先选择：
- 原文中最常用的表述
- 更标准、更常见、更适合作为课程节点的表述
3. 同义词之间不要建立任何关系，不要输出“对比”“衍生”“包含”。
4. 例如：
- percentiles 和 quantiles 若原文说明为同义词，只保留一个
- boxplot 和 box-and-whiskers plot 若原文说明为同义词，只保留一个

四、实体类型规则
实体类型（type）只能是以下五种之一：
- concept   核心概念、统计量、图表、数据表示、理论对象、数据类型
- algorithm 算法、模型、方法、检验、估计方法、推理机制
- task      任务、目标、应用场景
- process   流程、阶段、步骤
- tool      软件、框架、库、平台、系统工具

补充约束：
- 可视化图表、统计图形、列联表、箱线图、直方图优先标为 concept
- 方法、模型、检验、回归、推理方法优先标为 algorithm
- 软件、框架、库、平台才标为 tool
- 如果拿不准类型，优先标为 concept

五、definition 规则
1. definition 必须基于原文，用一句简短中文说明“这个概念与其他概念的区别”。
2. definition 不能只是把原词翻译成中文，也不能写成“某种方法”“某个概念”“与XX相关”这种空话。
3. definition 不能把一长串同义词硬塞进去。
4. 如果原文对该实体没有提供足够定义信息，可写空字符串 ""，不要编造。
5. definition 不超过 50 个字。

六、关系抽取规则
1. 只抽取原文有直接文本依据的关系。
2. 只有在原文出现明确的定义句、分类句、用途句、组成句、比较句时，才允许抽关系。
3. 如果两个概念只是同时出现，没有明确逻辑连接，不要建立关系。
4. 关系要少而精；宁可少，也不要泛化。
5. 如果没有明确关系，relations 可以为空数组。

关系类型（relation）只能是以下六种之一：
- 包含       source 是上位类/整体/分类结果，target 是下位类/组成部分
- 依赖       source 的理解或使用依赖 target
- 应用于     source 被应用到 target 任务或场景
- 衍生       target 由 source 发展、变形或扩展而来
- 前置条件   学习 source 前必须先掌握 target
- 对比       source 与 target 是两种不同且被原文明确比较的概念/方法

七、关系判定的硬规则
1. “A 是一种 B” => source=B, target=A, relation=包含
2. “A 包括 B” 或 “A 由 B 构成” => source=A, target=B, relation=包含
3. 同义词、别名、简称、翻译对应词，不能建立关系
4. “可以看作”“类似于”“是…的平滑版本”这类表达，只有在原文非常明确时才考虑输出；否则不要强行用“衍生”
5. 如果你无法准确判断方向或关系类型，就不要输出这条关系

八、漏抽控制
如果段落中存在以下信号，必须优先检查是否有核心实体被漏掉：
- KEY TERMS、关键术语列表、概念列表、分类列表
- “包括”“分为”“是一种”“是指”“用于”“表示”“衡量”
- 对某个术语进行集中定义或比较

即使段落混有数字、例子、代码、州名或噪声，也不能因此把真正的核心概念全部漏掉。

九、输出前自检
输出前请逐条检查：
- 是否漏掉了原文明确列出的核心术语
- 是否把同义词拆成多个节点
- 是否把评测维度、属性、例子误抽成实体
- 是否存在原文未支持的实体
- definition 是否真正有区分度
- 每条关系是否有明确文本依据
- “包含”方向是否正确

十、输出要求
1. 只返回合法 JSON，不要解释，不要 Markdown 代码块。
2. 不要输出重复实体名。
3. 每条关系的 source 和 target 必须引用 entities 中已有 id。

输出格式：
{{
  "entities": [
    {{"id": "e1", "name": "实体名称", "type": "concept", "definition": "基于原文内容的简短定义"}}
  ],
  "relations": [
    {{"source": "e1", "target": "e2", "relation": "包含"}}
  ]
}}

段落来源：《{book_title}》- {chapter_title}

段落内容：
{text}"""

