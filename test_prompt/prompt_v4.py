"""Prompt v4 for task 9 iterative refinement."""

PROMPT_VERSION = "v4"

PROMPT_TEMPLATE_V4 = """你是一个专业的知识图谱构建助手，负责从数据科学与人工智能教材中提取“适合教学使用”的知识图谱。

你的目标是：在不幻觉、不乱连边的前提下，优先保留本段真正有教学价值的核心概念与最关键关系。

你必须遵守以下总原则：
1. 只依据“本段正文”抽取，不要把其他小节、图注、回顾列表、跨段背景知识当成本段核心内容。
2. 同时出现“核心概念”和“例子/实例/指标/噪声”时，优先抽核心概念。
3. 如果原文没有明确定义、分类、比较或说明关系，不要硬抽。
4. 宁可少而准，不要多而乱。

一、优先抽取什么
优先保留以下实体：
- 本段明确定义、解释、分类、比较的概念
- 本段 KEY TERMS / 关键术语中确实被本段正文继续说明的术语
- 数据类型、统计量、图表、模型、方法、任务、工具
- 本段主旨直接围绕的概念

二、不要抽取什么
1. 不要抽取只是在本段顺带提及、但没有被本段重点说明的回顾性概念。
2. 不要把图例、图注、文献引用、例子中的具体实例（如 BERT、GPT-3、MYCIN）优先于上位核心概念抽取；如果上位概念更重要，只保留上位概念。
3. 不要把评测指标、评价维度、性质、属性、解释维度、监管要求条目单独当作实体，除非原文明确把它当成独立概念进行定义和展开。
4. 不要把州名、数字、人名、案例细节、表格列名、符号、章节标号抽成实体。
5. 不要为同义词、别名、简称、译名建立多个节点。
6. 如果某词仅是极基础、几乎不言自明、且本段没有进一步教学展开，优先不抽。

三、同义词规则
1. 如果原文明确说 synonyms / 同义词 / 又称 / 也称 / 别名，只保留一个主术语。
2. 主术语优先选择更标准、更常见、在课程中更适合作为节点的那个。
3. 同义词之间不要建立任何关系。

四、实体类型
实体类型（type）只能是：
- concept
- algorithm
- task
- process
- tool

类型判定：
- 数据类型、统计量、图表、理论对象、评估主题 => concept
- 模型、方法、检验、推理方法 => algorithm
- 任务、目标、应用场景 => task
- 流程、阶段、步骤 => process
- 软件、框架、库、平台 => tool

补充：
- outcome、feature、record、categorical data、binary variable 这类是 concept，不是 task
- 箱线图、直方图、频率表、密度图、列联表这类是 concept，不是 tool
- 主动干预、反事实推理、逻辑回归、线性回归这类优先是 algorithm

五、definition 规则
1. definition 必须忠于本段原文，不能用常识补充。
2. definition 必须写出该实体在本段中的核心含义或区分点。
3. 不要把“很多场景会这样用”“常见情况是”写成定义，除非原文明确就是这样定义的。
4. 不要把一串同义词堆进 definition。
5. 如果本段没有给出足够定义信息，写空字符串 ""。
6. definition 不超过 45 个字。

六、关系抽取规则
1. 只有在本段出现明确的定义句、分类句、组成句、用途句、比较句时，才允许抽关系。
2. 如果只是并列提到、背景提到、趋势描述、评价说明，不要建立关系。
3. 如果你无法确定关系类型或方向，就不要输出。
4. relations 为空是允许的，但如果本段存在非常明确的关系，不应漏掉。

关系类型（relation）只能是：
- 包含
- 依赖
- 应用于
- 衍生
- 前置条件
- 对比

七、关系硬规则
1. “A 是一种 B” => source=B, target=A, relation=包含
2. “A 包括 B / A 分为 B / A 由 B 构成” => source=A, target=B, relation=包含
3. “A 用于 B” => source=A, target=B, relation=应用于
4. 同义词、别名、不同说法 => 不建关系
5. “可以看作”“类似于”“平滑版本”这类弱表述，默认不要输出“衍生”，除非原文明确表示发展、扩展、变形
6. “领域包含某指标/维度/关注点”通常不要建“包含”，因为这些常是属性而非节点

八、漏抽控制
如果本段正文中明确出现以下内容，你必须检查是否漏抽：
- categorical data / binary variable / indicator variable 这类数据类型概念
- equal-size bins / equal-count bins / histogram / frequency table 这类数据分布概念
- 可解释AI的原因、风险、信任、监管、分类维度这类导论段落主旨概念

九、段落主旨校验
输出前先判断：本段最主要在讲什么？
- 如果你抽出的实体大多是例子、实例、次要术语，而主旨概念没有出现，说明结果有问题，应重做筛选。

十、输出前最终自检
逐条检查：
- 是否漏掉本段主旨概念
- 是否把章节回顾或别段概念误当成本段核心实体
- 是否把评测维度、属性、例子误抽成实体
- 是否把同义词拆成多个节点
- definition 是否紧贴本段原文
- 关系是否有直接文本依据
- “包含”方向是否正确

十一、输出要求
1. 只返回合法 JSON，不要解释，不要 Markdown 代码块。
2. 不要输出重复实体名。
3. 每条关系的 source 和 target 必须引用 entities 中已有 id。

输出格式：
{{
  "entities": [
    {{"id": "e1", "name": "实体名称", "type": "concept", "definition": "基于本段原文的简短定义"}}
  ],
  "relations": [
    {{"source": "e1", "target": "e2", "relation": "包含"}}
  ]
}}

段落来源：《{book_title}》- {chapter_title}

段落内容：
{text}"""

